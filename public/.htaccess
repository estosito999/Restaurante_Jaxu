# /public/.htaccess
<IfModule mod_rewrite.c>
  RewriteEngine On
  # URLs limpias hacia public/index.php
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^ index.php [QSA,L]
</IfModule>

